# List out all the string methods that you know :
# Follow the format :
# method()
# Definition : 
# Example :A suitable example
# For example :

# str()

"""Parameter :Has 3 (object,encoding--(UTF8 default),errors(on encoding failure))"""
"""Usage : Converts all objects that are passed to a string"""
"""Example : x = str(1234) """

# 1
#var.capitalize()

"Usage : It makes the first letter of the string capital"
"eg. n.capitalize()"

# 2
#var.casefold() and var.lower()

"Usage: These makes the the string to lower case"
"eg. n.casefold() and n.lower()"

# 3
# var.upper()

"Usage: it makes all the letters into capital."
"eg. n.upper()"

# 4
# var.swapcase()

"Usage: It swaps all the cases"
"eg. n.swapcase"

# 5
# var.count()

"Usage: It counts the indexes of the string"
"eg. n.count()"

# 6
# var.title()

"Usage: it makes the first letters of all the words in a string"
"eg."

# 7
# "item".join(var)

"Usage: inserts 'item' between all the things of the string"
"eg. '#'.join(n)"

# 8
# var.strip(Item you want to cut)

"Usage: It can cut any thing from the string"
"eg. n.strip(h)"

# 9
# var.rstrip(Item you want to cut from the right side of the string)

"Usage: It can cut any thing from the string's right side"
"eg. n.rstrip(o)"

# 10
# var.lstrip(Item you want to cut from the left side of the string)

"Usage: It can cut any thing from the string's left side"
"eg. n.lstrip(o)"

# 11
# var.split()

"Usage: It splits all the words in diffrent list cells."
"eg. n.split()"

# 12
# var.identifier()

"Usage: If the str starts from a letter and does not contain a special character this will be True"
"eg. n..identifier()"

# 13
# var.isalpha()

"Usage: Returns 'True' if a string only contains letters."
"eg. n.isalpha()"

# 14
# var.isalnum()

"Usage: Returns 'True' if a string contains letters and numbers both but not spaces and any special characters."
"eg. n.isalnum()"

# 15
# var.isdigit()

"Usage: Returns True if all characters in the string are digits"
"eg. n.isdigit()"

# 16
# var.islower()

"Usage: Returns True if all characters in the string are lower case"
"eg. n.islower()"

# 17
# var.isnumeric()

"Usage: Returns True if all characters in the string are numeric"
"eg. n.isnumeric()"

# 18
# var.isprintable()

"Usage: Returns True if all characters in the string are printable"
"eg. n.isprintable()"

# 19
# var.istitle()

"Usage: Returns True if the string follows the rules of a title"
"eg. n.istitle()"

# 20
# var.isupper()

"Usage: Returns True if all characters in the string are upper case"
"eg. n.isupper()"

# 21
# var.replace()

"Usage: Returns a string where a specified value is replaced with a specified value"
"eg. n.replace()"
